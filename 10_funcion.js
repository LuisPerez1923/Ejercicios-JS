/* Busca la sintaxis de una funcion y realiza el ejemplo de una */

/* Bonus */

/*function() permite definir un bloque de codigo, darle
un nombre usandolo como una palabra clave y puede 
incluir uno o mas parametros*/

let x = valores(4, 3, 2);   

function valores(a, b, c) {
  console.log(a * b + c);             
}